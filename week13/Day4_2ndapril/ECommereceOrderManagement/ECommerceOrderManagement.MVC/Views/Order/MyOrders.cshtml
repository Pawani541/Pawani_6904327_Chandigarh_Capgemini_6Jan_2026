@model List<ECommerceOrderManagement.MVC.Models.OrderViewModel>
@{ ViewData["Title"] = "My Orders"; Layout = "~/Views/Shared/_Layout.cshtml"; }

<h2 class="mb-4"><i class="fas fa-box me-2"></i>My Orders</h2>
@if (!Model.Any())
{
    <div class="alert alert-info">No orders yet. <a href="/">Shop now!</a></div>
}
@foreach (var o in Model)
{
    <div class="card mb-3 p-3">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h5 class="mb-1">Order #@o.Id</h5>
                <small class="text-muted">@o.OrderDate.ToString("dd MMM yyyy, hh:mm tt")</small>
            </div>
            <div class="text-end">
                <span class="badge bg-warning text-dark fs-6">@o.Status</span>
                <h5 class="text-primary mt-1">₹@o.TotalAmount.ToString("N0")</h5>
            </div>
        </div>
        <hr/>
        @foreach (var item in o.Items)
        {
            <div class="d-flex justify-content-between">
                <span><i class="fas fa-laptop me-2"></i>@item.ProductName × @item.Quantity</span>
                <span class="fw-bold">₹@((item.UnitPrice * item.Quantity).ToString("N0"))</span>
            </div>
        }
    </div>
}


